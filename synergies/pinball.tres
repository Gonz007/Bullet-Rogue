[gd_resource type="Resource" script_class="SynergyData" load_steps=2 format=3]
[ext_resource type="Script" path="res://scripts/synergies/synergy_data.gd" id="1"]
[resource]
script = ExtResource("1")
id = "pinball"
display_name = "PINBALL"
description = "Split and Bounce grant every round an extra rebound."
required_card_ids = ["bounce", "split"]
effect_type = "extra_bounce"
